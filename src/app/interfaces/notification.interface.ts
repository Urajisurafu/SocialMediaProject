export interface NotificationFriends {
  creatorId: string;
  timestamp: any;
}

export interface NotificationLikes {
  creatorId: string;
  notificationLikeId: string;
  postId: string;
  timestamp: any;
}
